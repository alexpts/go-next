- Подумать над адаптерами, чтобы fasthttp или другие обработчики и request использовать можно было.
- В gorilla есть сахар методы для ограничения
```r.HandleFunc("/products", ProductsHandler).Host("www.example.com").Methods("GET", "PUT"). Schemes("http")```. Можно иметь такую фабрику
- возможно стоит всю логику из store layer выкинуть, остави только метод addLayer, все что касается создания слоев, store не знает, но нужен скаха тогда, чтобы лаконично писать
